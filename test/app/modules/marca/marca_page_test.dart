import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:facturamobile/app/modules/marca/marca_page.dart';

main() {
  testWidgets('MarcaPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MarcaPage(title: 'Marca')));
    //  final titleFinder = find.text('Marca');
    //  expect(titleFinder, findsOneWidget);
  });
}
