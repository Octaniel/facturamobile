import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:facturamobile/app/modules/tipo_produto/tipo_produto_page.dart';

main() {
  testWidgets('TipoProdutoPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TipoProdutoPage(title: 'TipoProduto')));
    //  final titleFinder = find.text('TipoProduto');
    //  expect(titleFinder, findsOneWidget);
  });
}
