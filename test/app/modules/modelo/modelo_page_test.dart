import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:facturamobile/app/modules/modelo/modelo_page.dart';

main() {
  testWidgets('ModeloPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ModeloPage(title: 'Modelo')));
    //  final titleFinder = find.text('Modelo');
    //  expect(titleFinder, findsOneWidget);
  });
}
