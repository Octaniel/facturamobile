import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:facturamobile/app/modules/cliente/cliente_controller.dart';
import 'package:facturamobile/app/modules/cliente/cliente_module.dart';

void main() {
  initModule(ClienteModule());
  // ClienteController cliente;
  //
  setUp(() {
    //     cliente = ClienteModule.to.get<ClienteController>();
  });

  group('ClienteController Test', () {
    //   test("First Test", () {
    //     expect(cliente, isInstanceOf<ClienteController>());
    //   });

    //   test("Set Value", () {
    //     expect(cliente.value, equals(0));
    //     cliente.increment();
    //     expect(cliente.value, equals(1));
    //   });
  });
}
