import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:facturamobile/app/modules/serie/serie_page.dart';

main() {
  testWidgets('SeriePage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SeriePage(title: 'Serie')));
    //  final titleFinder = find.text('Serie');
    //  expect(titleFinder, findsOneWidget);
  });
}
